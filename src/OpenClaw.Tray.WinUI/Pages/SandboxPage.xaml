<?xml version="1.0" encoding="utf-8"?>
<Page x:Class="OpenClawTray.Pages.SandboxPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <ScrollViewer Padding="24">
        <StackPanel Spacing="16" HorizontalAlignment="Stretch" MaxWidth="900">

            <TextBlock Text="🛡 Sandbox"
                       Style="{StaticResource TitleTextBlockStyle}" />
            <TextBlock Text="Controls what AI agents can see and do on this machine when they run commands. By default the sandbox is tight — only what's checked here is allowed."
                       TextWrapping="Wrap"
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}" />

            <!-- Status banner -->
            <Border x:Name="StatusBanner"
                    Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1"
                    CornerRadius="6"
                    Padding="12">
                <StackPanel Spacing="4">
                    <TextBlock x:Name="StatusBannerHeader" Text="Sandbox availability: probing…"
                               Style="{StaticResource BodyStrongTextBlockStyle}" />
                    <TextBlock x:Name="StatusBannerDetail" Text=""
                               TextWrapping="Wrap"
                               Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                </StackPanel>
            </Border>

            <!-- Master toggle -->
            <ToggleSwitch x:Name="SandboxEnabledToggle"
                          Header="Sandbox agent commands"
                          OffContent="Off — commands run directly on your machine"
                          OnContent="On — commands run inside an MXC AppContainer"
                          Toggled="OnSandboxEnabledToggled" />
            <TextBlock x:Name="SandboxOffWarning"
                       Text="⚠️ With the sandbox off, every agent command runs as you, with full access to your files, network, and settings."
                       TextWrapping="Wrap"
                       Foreground="OrangeRed"
                       Visibility="Collapsed" />

            <!-- Network -->
            <Expander IsExpanded="True" HorizontalAlignment="Stretch" HorizontalContentAlignment="Stretch">
                <Expander.Header>
                    <TextBlock Text="📡 Network" Style="{StaticResource BodyStrongTextBlockStyle}"/>
                </Expander.Header>
                <StackPanel Spacing="12" Padding="4">
                    <ToggleSwitch x:Name="NetInternetToggle"
                                  Header="Allow internet"
                                  OffContent="Off — agent cannot reach any website"
                                  OnContent="On — agent can call any HTTPS site"
                                  Toggled="OnNetInternetToggled" />
                    <TextBlock Text="Local network (LAN) access is intentionally not exposed — MXC has not yet validated the privateNetworkClientServer capability."
                               TextWrapping="Wrap"
                               FontSize="11"
                               Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                </StackPanel>
            </Expander>

            <!-- Files -->
            <Expander IsExpanded="True" HorizontalAlignment="Stretch" HorizontalContentAlignment="Stretch">
                <Expander.Header>
                    <TextBlock Text="📁 Files" Style="{StaticResource BodyStrongTextBlockStyle}"/>
                </Expander.Header>
                <StackPanel Spacing="12" Padding="4">
                    <TextBlock Text="The agent can read/write inside a temporary folder by default. Add user folders here to grant more access. SSH keys, browser data, and OpenClaw's own settings are always blocked."
                               TextWrapping="Wrap"
                               Foreground="{ThemeResource TextFillColorSecondaryBrush}" />

                    <Grid ColumnSpacing="8" RowSpacing="6">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="160" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Grid.Column="0" Text="Documents" VerticalAlignment="Center"/>
                        <ComboBox x:Name="DocsAccessCombo" Grid.Row="0" Grid.Column="1"
                                  HorizontalAlignment="Stretch"
                                  SelectionChanged="OnDocsAccessChanged">
                            <ComboBoxItem Content="🚫 Blocked" Tag="None" />
                            <ComboBoxItem Content="🔒 Read only" Tag="ReadOnly" />
                            <ComboBoxItem Content="✏️ Read &amp; write" Tag="ReadWrite" />
                        </ComboBox>

                        <TextBlock Grid.Row="1" Grid.Column="0" Text="Downloads" VerticalAlignment="Center"/>
                        <ComboBox x:Name="DownloadsAccessCombo" Grid.Row="1" Grid.Column="1"
                                  HorizontalAlignment="Stretch"
                                  SelectionChanged="OnDownloadsAccessChanged">
                            <ComboBoxItem Content="🚫 Blocked" Tag="None" />
                            <ComboBoxItem Content="🔒 Read only" Tag="ReadOnly" />
                            <ComboBoxItem Content="✏️ Read &amp; write" Tag="ReadWrite" />
                        </ComboBox>

                        <TextBlock Grid.Row="2" Grid.Column="0" Text="Desktop" VerticalAlignment="Center"/>
                        <ComboBox x:Name="DesktopAccessCombo" Grid.Row="2" Grid.Column="1"
                                  HorizontalAlignment="Stretch"
                                  SelectionChanged="OnDesktopAccessChanged">
                            <ComboBoxItem Content="🚫 Blocked" Tag="None" />
                            <ComboBoxItem Content="🔒 Read only" Tag="ReadOnly" />
                            <ComboBoxItem Content="✏️ Read &amp; write" Tag="ReadWrite" />
                        </ComboBox>
                    </Grid>

                    <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                            BorderThickness="1"
                            CornerRadius="6"
                            Padding="8">
                        <StackPanel Spacing="8">
                            <TextBlock Text="Custom folders" Style="{StaticResource BodyStrongTextBlockStyle}"/>
                            <ListView x:Name="CustomFoldersList"
                                      MaxHeight="180"
                                      SelectionMode="None"
                                      Visibility="Collapsed">
                                <ListView.ItemTemplate>
                                    <DataTemplate>
                                        <Grid ColumnSpacing="8" Padding="2,4">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Grid.Column="0" Text="{Binding Path}" TextTrimming="CharacterEllipsis" VerticalAlignment="Center"/>
                                            <TextBlock Grid.Column="1" Text="{Binding AccessLabel}"
                                                       FontSize="11"
                                                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                                       VerticalAlignment="Center"/>
                                            <Button Grid.Column="2" Content="✕" Tag="{Binding Path}"
                                                    Click="OnRemoveCustomFolder"
                                                    Padding="6,2" MinWidth="0" />
                                        </Grid>
                                    </DataTemplate>
                                </ListView.ItemTemplate>
                            </ListView>
                            <TextBlock x:Name="CustomFoldersEmpty"
                                       Text="No custom folders added."
                                       FontSize="12"
                                       Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <Button Content="➕ Add folder (read-only)" Click="OnAddCustomFolderReadOnly" />
                                <Button Content="➕ Add folder (read+write)" Click="OnAddCustomFolderReadWrite" />
                            </StackPanel>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Expander>

            <!-- Clipboard -->
            <Expander IsExpanded="True" HorizontalAlignment="Stretch" HorizontalContentAlignment="Stretch">
                <Expander.Header>
                    <TextBlock Text="📋 Clipboard" Style="{StaticResource BodyStrongTextBlockStyle}"/>
                </Expander.Header>
                <StackPanel Spacing="8" Padding="4">
                    <TextBlock Text="By default the sandbox cannot see or change your clipboard."
                               TextWrapping="Wrap"
                               Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                    <RadioButton x:Name="ClipboardNoneRadio" Content="🚫 None (default)" GroupName="Clipboard" Checked="OnClipboardChanged" Tag="None" />
                    <RadioButton x:Name="ClipboardReadRadio" Content="👁 Read — agent can see what you copied" GroupName="Clipboard" Checked="OnClipboardChanged" Tag="Read" />
                    <RadioButton x:Name="ClipboardWriteRadio" Content="✍ Write — agent can put text in your clipboard" GroupName="Clipboard" Checked="OnClipboardChanged" Tag="Write" />
                    <RadioButton x:Name="ClipboardBothRadio" Content="🔁 Read &amp; Write — both" GroupName="Clipboard" Checked="OnClipboardChanged" Tag="Both" />
                </StackPanel>
            </Expander>

            <!-- Limits -->
            <Expander IsExpanded="False" HorizontalAlignment="Stretch" HorizontalContentAlignment="Stretch">
                <Expander.Header>
                    <TextBlock Text="⏱ Limits" Style="{StaticResource BodyStrongTextBlockStyle}"/>
                </Expander.Header>
                <StackPanel Spacing="12" Padding="4">
                    <StackPanel Spacing="4">
                        <TextBlock x:Name="TimeoutLabel" Text="Max time per command: 30 sec"/>
                        <Slider x:Name="TimeoutSlider" Minimum="5" Maximum="300" StepFrequency="5"
                                Value="30" SmallChange="5" LargeChange="15"
                                ValueChanged="OnTimeoutChanged" />
                    </StackPanel>
                    <StackPanel Spacing="4">
                        <TextBlock Text="Max output size:" />
                        <ComboBox x:Name="MaxOutputCombo"
                                  HorizontalAlignment="Stretch"
                                  SelectionChanged="OnMaxOutputChanged">
                            <ComboBoxItem Content="1 MiB" Tag="1048576" />
                            <ComboBoxItem Content="4 MiB (default)" Tag="4194304" />
                            <ComboBoxItem Content="16 MiB" Tag="16777216" />
                            <ComboBoxItem Content="64 MiB" Tag="67108864" />
                        </ComboBox>
                    </StackPanel>
                </StackPanel>
            </Expander>

        </StackPanel>
    </ScrollViewer>
</Page>
